# Cloudflare Workers 配置文件
name = "gedan"
main = "_worker.js"
compatibility_date = "2024-01-01"

# Workers 配置(可选)
# 如果只需要静态部署,推荐使用 Cloudflare Pages

[site]
bucket = "."

# 如果需要绑定 KV 存储或其他服务,取消注释以下配置
# [[kv_namespaces]]
# binding = "SONGS_KV"
# id = "your-kv-namespace-id"